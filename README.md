
En el proyecto cmd:

1. py -m venv venv
2. venv\Scripts\activate
3. pip install -r requirements.txt si es necesario
4. ctrl + shift + p para cambiar el interpretador
5.  uvicorn main:app --reload


Finalizar: deactivate